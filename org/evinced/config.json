{
  "severity_overrides": {
    "accessibility": { "min_severity": "medium" },
    "security": {
      "title_patterns": {
        "unbounded memory": { "max_severity": "low", "reason": "Downgrade when spec explicitly addresses memory bounds or defers LRU" },
        "global state singleton": { "max_severity": "low", "reason": "Downgrade when spec explicitly chooses singleton as design pattern" }
      }
    }
  },
  "test_command": "pnpm test"
}
